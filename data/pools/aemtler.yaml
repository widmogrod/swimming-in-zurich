# Schulschwimmanlage Aemtler — thin CROSSWALK only (delete-curated-schedule-tier S3).
#
# A `school` pool: NOT scraped by `scrape_indoor_facilities` (WFS-`indoor` only) and it publishes
# no Belegungsplan, so it carries no lane binding at all. After the strip it is permanently
# schedule-less → `ScheduleFreshness.NO_SOURCE` (no rules, non-scrapeable kind). Identity, address
# and geo come from the WFS roster. No basin declares a crosswalk fact, so `basins` is empty.

facility_id: "schulschwimmanlage-aemtler"

basins: []
