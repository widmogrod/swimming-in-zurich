# Schulschwimmanlage Aemtler — thin CROSSWALK only (delete-curated-schedule-tier S3).
#
# A `school` pool, and one of the four `etl.scrape.declared_sources` selects (it owns its page
# rather than sharing the generic hallenbaeder.html overview), so its timetable IS scraped and it
# reads `ScheduleFreshness.SCRAPED` — school-access-vocabulary S2, 2026-08-05. It publishes no
# Belegungsplan, so it carries no lane binding at all. Identity, address and geo come from the WFS
# roster. No basin declares a crosswalk fact, so `basins` is empty.

facility_id: "schulschwimmanlage-aemtler"

basins: []
